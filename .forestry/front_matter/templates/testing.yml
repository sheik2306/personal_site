---
label: Testing
hide_body: true
fields:
- name: layout
  type: text
  config:
    required: false
  hidden: true
  label: layout
  default: class_article_layout
- name: title
  type: text
  config:
    required: false
  label: Title
- name: block_test
  type: blocks
  template_types:
  - testing
  - products
  - product
  config:
    min: 
    max: 
  label: block_test
- name: images
  type: field_group_list
  fields:
  - name: imagest
    type: image_gallery
    config: {}
    label: imagest
  config:
    min: 1
    max: 3
    labelField: 
  label: images
